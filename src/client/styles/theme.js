import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: '"Gobold", "Helvetica", "Arial", sans-serif',
    // ... other typography settings ...
  },
  // ... other theme settings ...
});

export default theme;